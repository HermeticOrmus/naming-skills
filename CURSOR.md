# Using this repo with Cursor

Copy `.cursor/rules/naming.mdc` to your project's `.cursor/rules/` directory or `CLAUDE.md` to the project root.

The discipline applies whenever you're naming a product, suite, or major component. The rule is `alwaysApply: false` since it's contextual.
